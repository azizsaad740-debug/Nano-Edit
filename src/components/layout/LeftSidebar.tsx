// ... (around line 14)
  const {
    brushState, setBrushStatePartial, selectiveBlurAmount, setSelectiveBlurAmount,
    currentEditState, layers, recordHistory, // FIX 29: Add recordHistory
  } = logic;
// ...