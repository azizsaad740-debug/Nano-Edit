import React, { useRef, useMemo } from 'react';
import { useLayerTransform } from "@/hooks/useLayerTransform";
import { RotateCw } from "lucide-react";
import { rasterizeLayerToCanvas } from "@/utils/layerUtils"; // Fix 165
import { useLayerTransform } from "@/hooks/useLayerTransform";
// ... (rest of file)